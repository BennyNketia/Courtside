export const name = '@courtside/mcp-server';

if (import.meta.url === `file://${process.argv[1]}`) {
  console.log(`${name}: not implemented yet — Sprint 1 wires up the Streamable HTTP transport.`);
}
