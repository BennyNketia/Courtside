export const name = '@courtside/runtime';

if (import.meta.url === `file://${process.argv[1]}`) {
  console.log(`${name}: not implemented yet — Sprint 2 wires up the LangGraph ReAct agent and SSE.`);
}
